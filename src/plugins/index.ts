import { configureAuth } from "./auth";
import { configureCache } from "./cache";
import { configureDb } from "./database";
import { configureEvents } from "./event";
import configureRateLimit from "./rate-limit";
import { configureSwagger } from "./swagger";

export {
  configureAuth,
  configureCache,
  configureDb,
  configureEvents,
  configureSwagger,
  configureRateLimit,
};
